{{#if singleVersion}}
    <button class="btn btn-primary js-generate">{{translate "Generate"}}</button>
{{else}}
    <button class="btn btn-primary js-save">{{translate "Save all"}}</button>
{{/if}}

<div class="customattributes"></div>

<section class="ngremotemedia-crop">
    <ul class="nav nav-pills inverted"></ul>
</section>

<div class="ngremotemedia-crop-container">
    <div class="image-wrap">
        <img src="{{media}}" />
    </div>
</div>
